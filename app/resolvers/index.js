/**
 * GraphQL Resolvers
 *
 * @author Davi Souto
 * @since 25/02/2020
 */

import usersResolvers from "./usersResolvers.js";

const resolvers = Object.assign({}, 
    usersResolvers
);

export default resolvers;